# 信長の野望 真戦 シミュレーター — Codex再構築仕様

## 最重要ゴール
既存のStreamlit版を壊さず、Next.js + TypeScriptベースのスマホ/PC対応Webアプリへ段階的に再構築する。
単なるUI変更ではなく、以下を最終的に一体化する。

- 武将画像付きの武将DB
- 名前検索
- 所持武将/凸/所持戦法管理
- 保存編成
- S1/S2/S3/PK(S4 四雄怒涛)までの全武将・全関連戦法
- 編成テンプレ参照
- 戦闘シミュレーター
- 実測に基づく信長真戦専用ダメージ計算
- 将来的な対策編成自動探索/AI編成

---

# 1. 既存資産を必ず再利用
現在のリポジトリには以下がある。

- `app.py` : 既存Streamlit版
- `data/generals_master.json`
- `data/skills_master.json`
- `data/status_master.json`
- `data/traits_master.json`

既存IDを壊さないこと。
既存Streamlit版は比較検証用として残すこと。
最初にUIロジック / データ読み込み / 戦闘ロジックを責務分解して調査すること。

---

# 2. 対象シーズンと武将範囲

## 必須範囲
**S1 / S2 / S3 / PKシーズン(S4「四雄怒涛」)までに実装された全武将を登録対象とする。**

「一部の有名武将だけ」では不可。
レア度を問わず、現在参照可能な全武将を対象にする。

2026-08時点の公開攻略DBでは全137名とされているため、まずは**137名を完全性チェックの目安**にすること。
ただし情報源の更新差異があり、別ページで129名表記なども見られるため、137という数字だけを絶対視せず、公式/攻略DB/既存データを突合して `data_audit.md` に差分を記録すること。

### S2追加武将（公開攻略DB上 8名）
- 黒田官兵衛
- 前田慶次
- 山県昌景
- 高橋紹運
- 里見義堯
- 相馬盛胤
- 大内義隆
- 瑞渓院

### S3追加武将（公開攻略DB上 8名）
- 柿崎景家
- まつ
- 真田昌幸
- 立花誾千代
- 鈴木佐大夫
- 安宅冬康
- 伊達輝宗
- 加藤嘉明

### PK/S4「四雄怒涛」追加武将（公開先行情報上 8名）
- 伊達政宗
- 長野業正
- 佐竹義重
- 三好実休
- 藤林正保
- 浦上宗景
- 堀直政
- 大久保長安

S1については「S2以降追加分を除いた基礎ロスター」を既存マスターと公開DBで照合し、漏れなく収録する。

---

# 3. 武将マスター拡張

`generals_master.json` は既存IDを維持しつつ、必要に応じて以下の情報を追加できるschemaへ拡張する。

```ts
interface General {
  unit_id: string
  name: string
  reading?: string
  aliases?: string[]
  faction?: string
  rarity?: number
  season_added?: "S1" | "S2" | "S3" | "S4"
  cost?: number
  gender?: string
  image?: string
  base_stats: {
    str?: number
    int?: number
    lea?: number
    spd?: number
    pol?: number
    cha?: number
  }
  growth_stats?: Partial<Record<"str"|"int"|"lea"|"spd"|"pol"|"cha", number>>
  troop_bonus?: Record<string, number>
  unique_skill_id?: string
  limit_break?: unknown
}
```

S1〜S4を切り替えると、そのシーズン時点で使用可能な武将のみ表示できるようにする。
S4選択時はS1〜S4累積ロスターを表示する。

---

# 4. 武将画像UI

武将選択は「名前だけのselect」を廃止し、**顔画像 + 武将名**を基本にする。

例:

```text
[顔画像]
黒田官兵衛
4凸 / 弓
```

画像は `public/characters/` に格納する前提。
`image` が無い武将はプレースホルダーを出す。
画像欠損で画面を壊さない。
外部サイトの画像を無断自動スクレイピング/ホットリンクしない。
利用許可確認済み画像を投入できる構造だけ先に完成させる。

### 検索
常設検索欄を用意。
- 黒田
- 官兵衛
- くろだ
- かんべえ
- 別名/alias

でヒットできる設計。
最低限は漢字部分一致をMVPで実装。

### 表示モード
- 画像グリッド
- コンパクトリスト
を切替可能にする。

---

# 5. UI構成

## PC
左サイドバーは3段階。
1. 通常（アイコン+文字）
2. アイコンのみ
3. 完全非表示

状態をLocalStorage保存。

## スマホ
常設左サイドバーは禁止。
下部タブ:
- ホーム
- 編成
- シミュ
- DB
- 設定/マイ

細かい項目はドロワー。

## ホーム
最初に以下の4導線を大きく表示。
- 手持ちから編成を作る
- 敵編成への対策を探す
- 戦闘シミュレーション
- 武将・戦法を調べる

---

# 6. 編成UI

自軍3武将 / 敵軍3武将。
PCは3カード横並び、スマホはレスポンシブ。

各武将カードに最低限:
- 画像
- 名前
- 凸
- 兵種
- 固有戦法
- 戦法2
- 戦法3
- 兵学
- 特性
- ステ振り

編集はモーダル/ドロワーで行う。

---

# 7. 所持管理

ローカル保存でMVPを完成させる。

武将:
- 所持/未所持
- 凸数
- お気に入り

戦法:
- 所持/未所持
- 他編成で使用中

将来Supabase等へ移せるRepository層を用意するが、初期版で外部DB必須にしない。

---

# 8. こどもん系の編成例を「検証データ」として解析

公開されている「はてなの真戦攻略Wiki / はてなの真戦ブログ（こどもん）」のS1〜S4編成例を調査し、文章を丸ごと転載するのではなく、**構造化した編成データ + 出典URL**として保持する。

最低限見る対象:
- S1 強度別テンプレ編成
- S2 強度別テンプレ編成
- S3 最強テンプレ編成96選
- S4 最強テンプレ編成105選
- 武将個別の推奨編成ページ

保存例:

```ts
interface ReferenceFormation {
  id: string
  season: "S1"|"S2"|"S3"|"S4"
  tier?: string
  name?: string
  troop_type?: string
  members: Array<{
    general_id: string
    role: "leader"|"sub"
    skills: string[]
    stat_focus?: string
    academy?: string[]
  }>
  source_name: string
  source_url: string
  checked_at: string
}
```

## 目的
これらは「強いから答えをそのまま採用」するためではなく、以下に使う。

1. 実在するテンプレ構成でエンジンを回す
2. 発動順/状態/戦法相互作用を確認
3. シミュレーション結果が極端に不自然でないか確認
4. 将来のおすすめ編成/対策編成候補生成に使う

Tierや評価はシーズン/更新日付きで保持し、絶対値としてハードコードしない。

---

# 9. 三国志真戦の計算式をそのまま正解扱いしない

**重要:** 旧版が三國志真戦由来の式をベースにしている場合でも、その式を信長真戦の正解として固定しないこと。

現在公開されている別の信長真戦シミュレーター自身も「三國志真戦の公開計算式をベースにしており、実ゲームと異なる可能性がある」と明記している。

したがって、新エンジンでは以下の方針にする。

### 計算モード
- `legacy_sangokushi` : 旧版互換比較用
- `nobunaga_calibrated` : 信長真戦実測ベース
- `experimental` : 検証中

本番の標準は最終的に `nobunaga_calibrated` を目標にする。

---

# 10. 信長真戦専用ダメージ検証を優先

こどもんの2026-07実測記事では、対人約990本の検証から少なくとも以下が報告されている。

- 兵刃ダメージには最低保証（床）がある
- 兵刃ダメージは概念的に「最低保証 + 兵力由来 + 属性差由来」の構造
- 属性差のマイナスは兵力由来部分を削るが、最低保証は削れない
- 兵1000時の通常攻撃の最低保証は約18.75
- 戦法ダメージ側には別の最低保証基準（兵1000時 約20.5 × 戦法ダメージ率）が示唆される
- 表示値にはおよそ±4%の乱数と切り捨てがあるという推定
- 低兵力帯では床がフラット化し、高兵力帯では単純比例から少し外れる
- レベルによって属性1ポイントの寄与率が変化する観測がある

**これらは三國志真戦式と同一とは限らないため、信長真戦専用の検証項目として実装/テストすること。**

ただし、記事の推定式を無検証で丸写しして確定式にしない。
エンジン内の係数は設定化し、将来差し替え可能にする。

例:

```ts
interface DamageModelConfig {
  normalFloorAt1000: number
  tacticFloorAt1000: number
  randomVariance: number
  roundingMode: "floor" | "round"
  troopCurve: "linear" | "piecewise"
  levelAttributeScale: Record<number, number>
}
```

---

# 11. 兵種相性・状態・発動順も信長仕様で再検証

公開検証では信長真戦の兵種相性補正値として±12.5%という実測報告もある。
旧版の三國志式を流用している場合は必ず差分確認する。

また公開攻略DBでは、合戦開始前/戦闘ターンの処理順として概ね以下が整理されている。

合戦開始前:
- 武将特性
- 受動
- 装備スキル
- カード
- 陣形
- 兵種
- 指揮

戦闘ターン側:
- 能動
- 通常攻撃
- 突撃

同種では速度や戦法枠順が影響する。

さらに以下を個別に実装/確認する。
- 連撃
- 封撃
- 無策
- 麻痺
- 疲弊
- 混乱
- 威圧
- 挑発
- 牽制
- 回避
- 鉄壁
- 必中
- 破陣
- 反撃
- 離反
- 心攻
- 援護
- 肩代わり
- 洞察
- 先攻
- 継続ダメージ
- 浄化
- 回復不可
- S4萎縮

未対応効果はクラッシュさせず `UNSUPPORTED_EFFECT` として記録する。

---

# 12. 実測校正用の検証ハーネスを作る

今後ユーザーがゲーム内戦報を提供したら、エンジン予測と比較できるようにする。

```ts
interface BattleEvidence {
  source: "game_report" | "public_test" | "manual"
  attacker: {...}
  defender: {...}
  conditions: {...}
  observed: {
    damage?: number
    healing?: number
    proc?: boolean
    status?: string[]
  }
  notes?: string
}
```

テストで:
- 予測値
- 実測値
- 誤差率
を出す。

係数を直す場合、どの実測セットにより変更したかコミット/ドキュメントに残す。

---

# 13. シミュレーション結果UI

初期表示はシンプル。

- 勝率
- 敗率
- 引き分け率
- 平均残存兵力
- 平均ターン

詳細を折りたたみ:
- ターン別兵力
- ダメージ内訳
- 回復量
- 戦法発動回数
- 状態異常
- 行動順
- ログ
- 分布

1 / 100 / 1000回を選択可能。
seed固定デバッグ対応。
1000回でもUIを固めない（Web Worker等を検討）。

---

# 14. 技術構成

推奨:
- Next.js
- TypeScript
- React App Router
- Tailwind CSS
- shadcn/ui等
- Zustand
- Vitest/Jest
- LocalStorage/IndexedDB

戦闘エンジンは以下を比較して選ぶ。

A. PythonエンジンをAPIとして当面利用
- 旧版再利用が容易
- 検証しやすい

B. TypeScriptへ段階移植
- PWA/クライアント処理しやすい
- 将来保守性高い

推奨は**旧Pythonをgolden referenceとして残しつつ、新TSエンジンを段階移植し、同一seedの差分テストを作る**方式。

---

# 15. MVP完成条件

1. Next.jsで起動
2. PC/スマホ対応
3. サイドバー3段階
4. スマホ下部ナビ
5. 武将画像カード
6. 名前検索
7. S1〜S4シーズンフィルタ
8. S1〜PK/S4までの全武将をマスター化（完全性監査付き）
9. 自軍3/敵軍3選択
10. 戦法選択
11. 凸/兵種/兵学/特性/ステ設定
12. 既存マスター利用
13. 戦闘実行
14. 1/100/1000回
15. 結果サマリー + 折りたたみ詳細
16. 所持管理
17. 編成保存
18. 画像プレースホルダー
19. 旧版比較テスト
20. `legacy_sangokushi` と `nobunaga_calibrated` を分離
21. 公開テンプレを構造化した検証データを作れる
22. lint/typecheck/test成功
23. 既存Streamlit版を削除しない

---

# 16. Codexが最初にやること

コードを書く前に以下をレポートする。

1. 現行リポジトリ構造
2. `app.py` の責務分解
3. 再利用すべき戦闘ロジック一覧
4. 4つのmaster JSON schema概要
5. 現在の武将数
6. S1/S2/S3/S4別の武将数
7. 公開DB（目安137名）との不足/重複/名前差分
8. S2/S3/S4追加武将が既存masterに揃っているか
9. 既存ダメージ式の由来（どこが三國志式か）
10. 信長実測記事との矛盾点
11. Python API維持 vs TS移植比較
12. 採用方針
13. Phase 1で変更するファイル一覧

その後、次の順で進める。

- Phase 1: データ監査 + 戦闘式監査
- Phase 2: Next.js UI土台
- Phase 3: 全武将DB + 画像UI + 検索
- Phase 4: 編成UI
- Phase 5: 新戦闘エンジン + 旧版比較
- Phase 6: 所持/保存
- Phase 7: テンプレ構造化 + 対策探索の土台
- Phase 8: レスポンシブ/性能/テスト仕上げ

---

# 17. 調査時の参考ソース

以下は調査用。内容は更新されうるため、取得日・出典を記録する。

- はてなの真戦攻略Wiki 武将DB: https://www.sanguo-zhi.com/wiki/general/
- S2環境: https://www.sanguo-zhi.com/wiki/season/s2/
- S3環境: https://www.sanguo-zhi.com/wiki/season/s3/
- S4環境: https://www.sanguo-zhi.com/wiki/season/s4/
- 編成テンプレ横断: https://www.sanguo-zhi.com/wiki/templates/index/
- S1テンプレ: https://www.sanguo-zhi.com/entry/s1-template/
- 信長ダメージ実測: https://www.sanguo-zhi.com/entry/damage-01/
- 状態効果: https://www.sanguo-zhi.com/wiki/guide/status-effects/
- 戦法種別/発動順: https://www.sanguo-zhi.com/wiki/guide/tactic-types/

参照先の記事本文の大量転載は避け、構造化データ・数値・検証結果・出典URLとして利用すること。

---

# 最終形
「画像で武将を見つける → 名前検索でも探せる → 手持ちを登録 → 編成を組む → 1000回シミュ → 信長真戦の実測式で結果を見る → テンプレと比較 → 将来は敵編成に勝つ手持ち編成を自動探索」までを一つのアプリとして成立させる。
